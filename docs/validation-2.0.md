# Product Video 2.0 验证记录

验证日期：2026-09-11。环境：macOS、Node.js 26.8.1、Remotion 4.0.484。镜头库使用当时已锁定的依赖版本。

| 范围 | 已验证结果 |
| --- | --- |
| 镜头目录 | 157 张镜头卡、214 个画廊样式全部映射；218 个运行组件，无缺失映射 |
| 全库渲染 | 每个组件渲染起始、中间、结束帧，共 654 张 PNG；218 个组件全部完成 |
| 原有引擎 | 93 项 Python 回归测试通过，覆盖原生截图、操作、录屏与三维功能 |
| 镜头与配音整合 | 10 项新增 Python 测试通过，覆盖素材绑定、版本选择、真实音频边界、字幕、转场与独立音轨 |
| 运行库逻辑 | 15 种转场的端点与方向、内容替换范围、无效映射及镜头时长回归检查通过 |
| 构建与安装 | TypeScript/Vite 构建通过；安装包在空目录解压后 npm ci 和构建通过；本机 Python 2.0.0 依赖检查通过 |
| 工作台交互 | 文案编辑、实时预览、撤销、播放与暂停、音色搜索和旁白更新完成实际操作验证 |
| 工作台布局 | 1280×720 与 390×844 检查；窄屏无横向页面溢出；旁白面板焦点循环、Escape 关闭与焦点恢复通过 |
| 混合成片 | CLI 与工作台均导出 51.63 秒、1280×720、30 fps、H.264/AAC 视频；包含 3 段旁白、17 条字幕及 11 个画面片段 |
| 文件校验 | 两条导出路径均通过音轨、画面规格、时长与完整解码检查；工作台输出附带 .verify.json |

混合成片实际包含中文标题替换、截图发牌、纸张字卡、原生点击和拖动、三维设备与真实录屏。素材采用标明用途的示例界面，旁白复用此前的更新报告，用于验证两套能力在同一成片中配合。

## 验证边界

- 654 张画面证明全库可执行，并用于抽样视觉复核；它们不是 218 段完整视频的逐帧验收。
- 示例素材、固定截图切片和自定义字形需按目标产品调整。未验证每个组件对任意内容、任意画幅都能直接适配。
- 语音联调使用已生成的三段缓存。没有为本次测试重新调用所有音色，也未对新的付费语音请求或所有语言作验证。
- 成片的声音轨与字幕时序已核对，未进行独立的听感审听。正式发布前仍需复核目标产品的文案、素材与听感。
- 本记录覆盖上述 macOS 环境；不将其视为 Windows、Linux 或其他浏览器的完整验证。

## 复现入口

```sh
cd scripts/engine
PYTHONPATH=. .venv/bin/python -m unittest discover -s tests -v

cd ../../vendor/video-shotcraft/workbench
npm run test:integration
npm run build
```

全库抽帧通过 `workbench/scripts/product-video.mjs smoke REQUEST.json` 执行。请求指定 `publicDir`、输出目录、目录中全部 `demo-<Component>` 标识及可选的 `resume`；生成逐组件的 `coverage.json` 和起始、中间、结束帧 PNG。

项目验证使用引擎的 `check`、`prepare-motion`、`studio` 与 `render`。工作台旁白成功后检查分轨与时长，再通过“导出成片”取得当前剪辑和校验文件。
